# AndroidX is required by the AndroidX/Material dependencies used by the app.
android.useAndroidX=true
# No legacy support-library dependencies currently require Jetifier.
android.enableJetifier=false
